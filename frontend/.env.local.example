# Frontend Environment Variables
# These URLs should point to your backend services

# Backend (main service, port 8000)
# Server-side only (used by Next.js API routes)
BACKEND_URL=http://localhost:8000

# Client-side fallback (used when BACKEND_URL is not available)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# DT Backend (intraday trading service, port 8010)
# Server-side only (used by Next.js API routes)
DT_BACKEND_URL=http://localhost:8010

# Client-side fallback (used when DT_BACKEND_URL is not available)
NEXT_PUBLIC_DT_BACKEND_URL=http://localhost:8010

# Development
NODE_ENV=development
PORT=3000

