"""Phase 4.5: Contextual bandit allocator (shadow-first).

This package deliberately starts simple:
- It can maintain per-bot performance priors
- It can propose bot weights for a given context

The live system should keep this OFF until replay/shadow metrics prove it.
"""
